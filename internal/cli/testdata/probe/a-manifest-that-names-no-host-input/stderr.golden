hyper probe: the candidate set and local's hosts: grant intersect to 2 hosts, and ambiguous check declares no host-input:
  which host the request would reach is undecidable — the Manifest names an input carrying one, or local grants one
