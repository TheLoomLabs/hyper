kind: provider
provider: schema-unsupported-demo
schema-version: 1
class: local
capabilities: [http]
operations:
  run:
    kind: read
    deadline: 1h
    http:
      method: GET
      host: "{from-target}"
      path: /
    input: {"$ref": "#/definitions/x"}
