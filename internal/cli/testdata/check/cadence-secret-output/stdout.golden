FILE                                LINE  FIELD    ERROR_CODE             MESSAGE
.github/workflows/hyper-rotate.yml                 projection-stale       the Procedure rotate declares a Cadence and the working tree holds no file here — hyper project writes it
procedures/rotate.yaml              4     cadence  cadence-secret-output  cadence: reaches a Step whose Operation declares secret: output at some depth — the workflow project generates supplies no Secret sink, so the Refusal would land at every occurrence
