FILE                        LINE  FIELD                                     ERROR_CODE           MESSAGE
providers/local-shell.yaml  5     capabilities                              capability-reserved  capabilities: declares shell, which is reserved to the Providers hyper ships — an Extension may never hold it
providers/local-shell.yaml  11    operations.read.shell                     capability-reserved  operations.read uses shell, which is reserved to the Providers hyper ships — an Extension may never hold it
providers/local-shell.yaml  26    operations.mutate.shell                   capability-reserved  operations.mutate uses shell, which is reserved to the Providers hyper ships — an Extension may never hold it
providers/local-shell.yaml  40    operations.mutate_once.shell              capability-reserved  operations.mutate_once uses shell, which is reserved to the Providers hyper ships — an Extension may never hold it
providers/local-shell.yaml  55    operations.mutate_skip_if_recorded.shell  capability-reserved  operations.mutate_skip_if_recorded uses shell, which is reserved to the Providers hyper ships — an Extension may never hold it
providers/local-shell.yaml  70    operations.destroy.shell                  capability-reserved  operations.destroy uses shell, which is reserved to the Providers hyper ships — an Extension may never hold it
providers/local-shell.yaml  78    operations.destroy_once.shell             capability-reserved  operations.destroy_once uses shell, which is reserved to the Providers hyper ships — an Extension may never hold it
