kind: provider
provider: p46
schema-version: 1
class: widgetco
capabilities: [http]
operations:
  ping:
    kind: read
    http:
      method: GET
      host: "{from-target}"
      path: /ping
    record:
      over: $.body.result
      identity: $.id
      fields: {id: $.id}
