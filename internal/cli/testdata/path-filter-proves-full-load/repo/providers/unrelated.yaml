kind: provider
provider: unrelated
schema-version: 1
class: local
capabilities: [http]
extra-key: true
operations:
  op:
    kind: read
    deadline: 10s
    http:
      method: GET
      host: "{from-target}"
      path: /
    record:
      identity: $.host
      fields: {host: $.host}
