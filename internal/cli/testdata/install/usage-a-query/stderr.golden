hyper install: "https://providers.example.com/acme/dns.yaml?token=t" is not a ref: a ref carries no query and no fragment — a ref is recorded, rendered and typed again, and either would let two callers name one set of bytes two ways
