kind: procedure
procedure: watch-many
targets: [local]
steps:
  - id: status
    definition: uptime-check
    operation: check_http
    target: local
    over:
      values: [status.hyper.dev, cert.hyper.dev]
    args:
      host: {item: $}
