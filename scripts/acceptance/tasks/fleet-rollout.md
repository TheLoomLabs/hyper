Three machines sit under `fleet/`, each with the version of the payload it is
currently running, and `fleet/wanted` names the version they are all supposed to
be on.

Get them all onto it, through `hyper`. A machine moved by hand is one this
repository has no account of, and the account is half of what I want out of this.

When you are done every file under `fleet/` will read the same, and nothing there
will still say what any machine was on beforehand. This repository has to be able
to say it anyway. Make that true, then tell me two things. Which machines were
behind, and what each of them was on. And what this repository's account of the
rollout now amounts to — what is in it, and which of it is something you did
rather than something you only looked at.

Take this as the approval you are told to wait for. Once `hyper check` is clean and
`hyper review` reads the way you meant it, run it — you do not need to come back to
me in between.
